class APP2   // Creats a new data type
{
	//CSC marks the Main() as the entry point so that the runtime CLR calls it
	static void Main()
	{
		// Take input from the user through the console window
		System.String input = System.Console.ReadLine();

		//Display the output to  the user on the cosole Window
		System.Console.WriteLine ("Hello" + input);
	}
}